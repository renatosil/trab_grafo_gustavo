module.exports.fnCusto = (solucao = []) {
    let total;
    let ultima;
    let primeira;
    let vertice;

    let tamanho = solucao.length();

    for (let i = 0; i < (tamanho / 2); i++ ) {
        partida = 
    }
}